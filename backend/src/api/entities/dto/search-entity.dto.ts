import { ApiPropertyOptional } from '@nestjs/swagger';

// Add search properties as needed
export class SearchEntityDto {
  // Example:
  // @ApiPropertyOptional({ example: 'Entity Name', description: 'Name to search' })
  // name?: string;
}
