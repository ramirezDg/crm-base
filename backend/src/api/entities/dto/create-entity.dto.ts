import { ApiProperty } from '@nestjs/swagger';

// Add properties as needed for your entity
export class CreateEntityDto {
  // Example property:
  // @ApiProperty({ example: 'Entity Name', description: 'Name of the entity' })
  // name: string;
}
