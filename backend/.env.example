# ===========================
# PostgreSQL Database Config
# ===========================
# DB_HOST: Hostname or IP address of the PostgreSQL server.
# DB_PORT: Port number for PostgreSQL (default: 5432).
# DB_USERNAME: Username for authenticating with PostgreSQL.
# DB_PASSWORD: Password for the PostgreSQL user.
# DB_DATABASE: Name of the PostgreSQL database to use.
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=
DB_DATABASE=database

# ===========================
# Application Configuration
# ===========================
# PORT: Port where the Node.js app will run.
# NODE_ENV: Application environment ('development', 'production', etc.).
PORT=3000
NODE_ENV=development

# ===========================
# JWT Authentication
# ===========================
# JWT_SECRET: Secret key for signing JWT tokens.
# JWT_EXPIRES_IN: Token expiration time (e.g., '3600s' for 1 hour).
JWT_SECRET=mi_secret_key
JWT_EXPIRES_IN=7200s

# ===========================
# Other Settings
# ===========================
# LOG_LEVEL: Logging level ('debug', 'info', 'warn', 'error').
# CORS_ORIGIN: Allowed origin for CORS requests.
LOG_LEVEL=debug
CORS_ORIGIN=http://localhost:5173

# ===========================
# Mailer Configuration
# ===========================
# MAIL_HOST: SMTP server host.
# MAIL_PORT: SMTP server port.
# MAIL_USER: SMTP username.
# MAIL_PASS: SMTP password.
# MAIL_FROM: Default sender address.
MAIL_HOST=smtp.example.com
MAIL_PORT=587
MAIL_USER=usuario@example.com
MAIL_PASS=contraseña
MAIL_FROM="CRM Base" <no-reply@example.com>

# ===========================
# Ejemplo de uso de variables
# ===========================
# Puedes acceder a estas variables en tu aplicación Node.js usando process.env, por ejemplo:
# const dbHost = process.env.DB_HOST;
# const mailUser = process.env.MAIL_USER;